{
  "name": "jardin-de-barrio-bogota",
  "version": "1.0.0",
  "private": true,
  "description": "Estudio navegable de prefactibilidad para un jardín infantil de barrio en Bogotá",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "npm run build && node --test tests/site.test.mjs",
    "dev": "node scripts/serve.mjs"
  }
}
